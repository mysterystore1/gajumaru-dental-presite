# アクセス解析 導入メモ（辻堂がじゅまる歯科）

> 導入予定。2026-06-25 メモ起票。設置は本番公開フェーズで。測定ID確定後にこのファイルへ追記。

## 導入予定ツール
- **Microsoft Clarity**（無料）… ヒートマップ／セッション録画／スクロール・クリック分析。https://clarity.microsoft.com/lang/ja-jp
- **Google Analytics 4 (GA4)**（無料）… 流入・ページ・コンバージョン（予約/電話/問い合わせ）計測。
- 併用が定番（Clarity=行動の質的観察／GA4=量的計測）。

## 次の手順（設置時）
1. それぞれアカウント作成 → 計測ID/プロジェクトIDを取得（※IDはこのファイルに記録可。鍵ではない）。
2. 全ページ `<head>` にタグ設置。本サイトは静的HTML多数 → `_partials.html` 方式 or 各HTML共通headに一括注入できるか確認（main.js注入も検討）。
3. GA4のコンバージョン設定＝予約ボタン/tel:発信/contact送信 をイベント計測。
4. honban(プレビュー領域)と公開ルートで計測を分ける/除外する設計を確認（noindexのhonbanを誤計測しない）。

## ⚠️ 医療サイト特有の注意（GL・個人情報）
- **Clarityのセッション録画＝個人情報取得にあたる**。予約フォーム/問い合わせの**氏名・電話・メール入力欄はマスキング設定必須**（Clarityのmask設定 or `data-clarity-mask`）。録画に患者の入力が残ると個人情報漏えいリスク。
- **プライバシーポリシーに解析ツール利用を明記**（Cookie・録画・第三者送信）。`honban/privacy.html` の更新が必要。
- Cookie同意（バナー）要否を確認。最低限プライバシーポリシーで告知。
- 医療広告GLとは別論点だが、患者データ＝慎重に。

## 実装状況（2026-06-26）

### Clarity 導入済み・稼働確認済み（2026-06-26）
- **測定ID＝`xcwuquiftw`**（プロジェクト名「辻堂がじゅまる歯科 ホームページ」／業種=健康とウェルネス／URL=https://gajumaru-dental.com/）。
- 設置方式：両 `main.js` 先頭にClarity注入スニペット（`js/main.js`＝公開ルート／`honban/js/main.js`＝honban・**同一ID**）。全ページがbody末尾でmain.jsを読むため**全ページ一括**。honban＝8/1昇格後も同一IDで継続。
- **録画マスキング済（HTML側）**：問い合わせフォームの 氏名/メール/電話/本文 に `data-clarity-mask="true"`（ルート `index.html`＋`honban/contact.html`・各4欄）。
- **privacy.html 第6章 更新済**：Clarity利用・Cookie・録画・マスキング・第三者送信を明記（解析稼働と記載を一致）。
- 稼働確認（プレビュー実機）：ルート/honban 両方で `window.clarity=function`・clarity.msスクリプト実ロード・マスク4欄・既存JS（テーマ切替）健全・コンソールエラー0。

### Clarity側設定 完了（2026-06-26・医院用ブラウザで実施）
- **マスク表示モード＝厳密（Strict）**に設定済（全テキストをMicrosoftサーバへ送らずマスク＝医療の機微情報を最大保護）。※反映に最大1時間・遡及適用なし。
- プロジェクト名の誤字「ホープページ」→「ホームページ」修正・保存済。
- 業種＝健康とウェルネス／URL＝https://gajumaru-dental.com/。

### ▶ 残作業
1. **本番反映**：作業ブランチ feature/gajumaru-20260626 → main へPR・マージ（公開サイトで計測開始）。⚠main直push不可。
2. **IPブロック（本番反映後）**：出先の変動IPは無効化するため今は未登録。公開後に **医院LANの固定IP**（必要なら桑田/スタッフ宅IP）を Clarity 設定→IPブロック に登録し、関係者の確認アクセスを計測から除外。
3. （GA4は導入済み＝下記）。

### Google Analytics 4 (GA4) 導入済み（2026-06-26）
- **測定ID＝`G-DDTRX2HL8P`**（プロパティ「ホームページ」／アカウント「辻堂がじゅまる歯科」／業種=健康／タイムゾーン日本・通貨円／拡張計測ON）。ストリームID=15154303852。
- 設置：両 `main.js` のClarity直後に gtag.js 動的読込（`js/main.js`＝公開ルート／`honban/js/main.js`＝honban・**同一ID**）。Clarityと併用（GA4=量的計測／Clarity=質的観察）。honban昇格後も同一IDで継続。
- **イベント計測（入力値は送らずイベントのみ）**：
  - `generate_lead`＝問い合わせフォーム送信成功（fetch res.ok時）。
  - `click_to_call`＝電話発信（`tel:`リンククリック）。
- **privacy.html 第6章 更新済**：GA4利用・Cookie・入力情報を計測対象外・オプトアウト方法を明記。
- ⚠PII保護：URL/ページパスに個人情報を載せない（当サイトはフォームPOST送信でURLに氏名等が付かない）。

### ▶ GA4 残作業
1. **本番反映**：上記PRに同梱（main マージで公開サイトの計測開始）。
2. **キーイベント（コンバージョン）設定（ブラウザ作業）**：GA4管理→イベント→`generate_lead` と `click_to_call` を「キーイベント」にマーク（CV計測化）。データ受信後（最大48h）に表示される。
3. **【やる・確定】GA4 ↔ Clarity 相互リンク連携**（実施時期は問わず・後日でよい）：
   - 効果：GA4で見つけた数字の異常（例：あるページで離脱が多い）から、該当の **Clarity録画にワンクリックで飛べる**＝「気づき→原因確認」が直結。
   - 手順：Clarity → 設定（Settings）→ **統合（Integrations）** → **Google Analytics** を選び接続 → 連携するGoogleアカウント（info@gajumaru-dental.com）を認可 → GA4プロパティ「ホームページ（G-DDTRX2HL8P）」を選択。
   - 前提：設定自体はデータ前でも可。効果が出るのは両方にデータが溜まってから。医院用ブラウザ＋Claude拡張 or 桑田手動で実施可。
   - ⚠連携は同一Googleアカウント（info@）でClarity・GA4の両方にアクセスできること。
4. （任意）Google Search Console 連携（検索流入のキーワード把握）。

## 関連
- 本番公開フェーズで設置 → 公開後の MEO/SEO 改善（継続運用メニュー）の計測基盤になる。
