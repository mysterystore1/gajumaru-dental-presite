# 実装計画: 辻堂がじゅまる歯科 本番サイト化（マルチページ）

作成日: 2026-06-10
ステータス: 未着手
関連: DEC-001（独自ドメイン）/ DEC-002（静的構成維持）/ DEC-003（プレサイトを育てる）
参照: `HANDOFF.md` / `DECISIONS.md` / `TODO.md` / `c:\work\tasks\codex-dental-copy-audit.md`（文言トーン正本）

---

## 前提・方針（着手前に必読）

- **静的サイト維持（Node不使用 / DEC-002・003）**。ビルドツール・テンプレートエンジン非採用。`index.html` をブラウザで開けば確認可。`main` push で GitHub Pages 自動デプロイ。
- **公開URL `gajumaru-dental.com` 維持**（CNAME設定済み）。新規ページは同リポ内に追加。
- **デザインはプレサイトのトンマナ踏襲**。`css/style.css`（`:root` トークン: green `#6B9E7D` / accent `#D4915E` / Noto Sans JP + Zen Maru Gothic）を全ページ共有。新規CSSは同ファイル末尾にページ別ブロックで追記。配色・角丸・影・`.fade-in`・`.btn` 系を再利用。
- **全データ仮で骨組み構築 → 実データ後日差し替え**。院内写真は約2週間後。仮テキストは `（仮）` 明示、仮画像はプレースホルダー運用（後述）。
- **新規追加なし方針の例外**: マルチページ化で `header` / `footer` / `mobile-bar` を各ページに複製する必要あり（includesはNode無しでは不可）。同期方針を Task 1 で確定。

### 共通部品（ヘッダー/フッター/モバイルバー）の扱い — 重要

Node無しのため SSI / インクルードは使えない。3案を検討し **案A を採用**:

- **案A（採用）**: 共通HTMLを各ページに**コピー複製**。1ファイル（`_partials.html` = SSOTメモ）に最新版を保管し、変更時は全ページへ手動反映。ページ数が9前後で管理可能。デプロイ最速・依存ゼロ。
- 案B（不採用）: JSの`fetch`で`partials/header.html`を読み込み注入。`file://`直開きでCORS失敗・初期表示にちらつき・SEOでヘッダー内リンクが初期HTMLに出ない（MEO不利）。
- 案C（不採用）: ビルド導入。DEC-002/003に反する。

→ **ヘッダーのナビ項目・電話番号・住所など共通要素を変更したら、全ページに反映する**ことをルール化（Task 1 のチェックリスト＋HANDOFFに明記）。

### 仮データ・仮画像の扱い（全ページ共通ルール）

- **仮テキスト**: 既存踏襲で `（仮）` を語尾に付与。NAP（医院名/住所/電話）は後で一括置換できるよう表記統一（電話 `0466-XX-XXXX` / 住所 `〒251-0047 神奈川県藤沢市辻堂1-00-00（仮）`）。
- **仮画像（院内写真は約2週間後）**: 既存の `images/clinic-reception.svg` / `clinic-room.svg`（SVGプレースホルダー）を流用。新規に必要な箇所は同系統のSVGプレースホルダー or CSSグレーボックス（`alt`は本番想定文言を先に入れる）。差し替え時は `src` のみ変更。
- **差し替え一覧を `IMAGE_TODO.md` に集約**（Task 3）。本番写真到着時に参照する1枚マップ。
- **構造化データ/OGPの仮値**: 実NAP確定まで JSON-LD の `telephone` / `streetAddress` は**未記載のまま**（誤情報をMEOに出さない）。確定後 Task 13 で追記。

### 着手優先順位（このpage順で実装）

1. **トップ（既存index発展）** — ハブ。全ページへの導線がここから出る
2. **通院をためらう方へ★** — コンセプト体現・最重要・集患の核（文言正本あり）
3. **診療案内（5診療）** — SEOの主戦場「辻堂×診療」
4. **院長・当院紹介** → 5. **院内紹介** → 6. **料金表** → 7. **アクセス** → 8. **よくある質問**
9. お知らせ（一覧＋1記事の雛形）
※ 予約・お問い合わせは既存トップ内セクションを活かしつつ、Task 2 で導線（EPARK/LINE/電話/フォーム）を全ページ共通パーツ化

---

## フェーズ0: 基盤・共通化（マルチページの土台）

### Task 1: 共通部品SSOTとページ雛形を確定
- 対象: `_partials.html`（新規・コピー元メモ）, `PAGE_TEMPLATE.html`（新規・空ページ雛形）
- やること:
  - 既存 `index.html` の `header` / `footer` / `mobile-bar` を抜き出し `_partials.html` に「コピー元正本」として保存。冒頭に「変更したら全ページへ反映」の注意書き。
  - ナビ項目を本番マルチページ用に再定義（トップ / ためらう方へ / 診療案内 / 院長・当院 / 院内 / 料金 / アクセス / よくある質問 / お知らせ）。リンクは相対パス（`/` 直下構成 or サブディレクトリ構成をここで決定 → **ルート直下フラット構成採用**: `dental-anxiety.html` 等。理由: 静的・相対パス単純・MEO上のURL短さ）。
  - `PAGE_TEMPLATE.html`: `<head>`（meta/canonical/OGP/JSON-LD枠/CSS/font）＋ヘッダー＋`<main>`空＋フッター＋mobile-bar＋`<script src="js/main.js">` の空ページ雛形。
- 完了条件: `PAGE_TEMPLATE.html` をブラウザで開くとヘッダー/フッター/mobile-barが既存と同一表示。コンソールエラーなし。
- 依存: なし
- [x] 完了（2026-06-10: `_partials.html` / `PAGE_TEMPLATE.html` 作成。ナビ9ページ・フラットURL。1280pxでnav inline/9項目、footer 3カラム、JSエラーなしを確認）

### Task 2: 予約導線パーツを設計（EPARK / LINE / 電話 / フォーム）
- 対象: `_partials.html`（予約導線ブロック追記）, `css/style.css`（末尾 `/* 予約導線 */` ブロック）
- やること:
  - 4導線のボタン群HTMLを定義（EPARK・LINEは `href="#"` 仮リンク＋`data-todo`、電話 `tel:0466XXXXXX`、フォームはトップ`#contact`へアンカー）。
  - `.reserve-cta` クラスを新規追加（既存 `.btn` 流用、配色は green/accent トークン）。各ページ下部に差し込む想定。
  - mobile-bar に LINE ボタン追加可否を検討（現状 電話＋問い合わせの2枠 → 3枠化はTask後半で判断、まず据え置き）。
- 完了条件: 予約導線ブロックを `PAGE_TEMPLATE.html` に貼ると4導線が崩れず表示。仮リンクは `data-todo` で後追い可能。
- 依存: Task 1
- [x] 完了（2026-06-10: `.reserve` ブロック追加。EPARK/LINE は href="#"+data-todo、電話 tel:、フォームは index.html#contact。4カラム→767px以下2カラム。崩れなし確認）

### Task 3: 画像差し替え台帳を作成
- 対象: `IMAGE_TODO.md`（新規）
- やること: 本番写真が必要な全箇所を表で列挙（ページ / 用途 / 現在の仮ファイル / 差し替え時の`src`目印 / `alt`本番案）。院内写真（約2週間後）・外観・院長・各診療イメージを網羅。
- 完了条件: 表が全ページの画像を1枚で俯瞰でき、写真到着時にこれだけ見れば差し替え可能。
- 依存: なし（他Taskと並行可）
- [x] 完了（2026-06-10: `IMAGE_TODO.md` 作成。全15枠を ページ/用途/仮ファイル/差し替え目印/alt本番案/状態 で表化）

---

## フェーズ1: トップページ発展（既存index）

### Task 4: トップのナビ・導線をマルチページ対応に更新
- 対象: `index.html`
- やること:
  - ヘッダーナビのアンカー（`#greeting`等）を新ページへのリンクに差し替え（ためらう方へ/診療案内/院長/院内/料金/アクセス/よくあるご質問/お知らせ）。トップ内維持の項目はアンカーのまま。
  - `_partials.html` 正本とヘッダー/フッター/mobile-barを一致させる。
- 完了条件: トップのナビから各ページへ遷移できる（リンク先は後続Taskで作成。先にリンクだけ通す→404は許容、Task完了時点でメモ）。表示崩れなし。
- 依存: Task 1
- [x] 完了（2026-06-10: index のヘッダー/フッター/モバイルバーを `_partials.html` と一致させ9ページ構成に更新。ロゴ→index.html、CTA→怖い方へ、footer 3カラム+nav追加。リンク先ページは Task7+ で作成、現時点 dental-anxiety.html 以外は404許容）

### Task 5: トップに「ためらう方へ」誘導＋診療一覧カードを追加
- 対象: `index.html`, `css/style.css`（`/* トップ: 診療カード */`）
- やること:
  - ヒーロー直下 or 特徴セクション付近に「歯医者が怖い・ためらう方へ」共感バナー（コピー: 文言正本のヒーロー案「『怖い』を、我慢しなくていい。」系を仮採用）＋ボタンで `dental-anxiety.html` へ。
  - 5診療（一般/小児/予防/歯周病/ホワイトニング）のカードグリッドを追加。各カードから診療案内ページの該当アンカーへ。既存 `.features` グリッドのトンマナ踏襲。
- 完了条件: トップから「ためらう方へ」と各診療へ導線が出る。レスポンシブ（767px以下1列）で崩れない。
- 依存: Task 1, Task 4
- [x] 完了（2026-06-10: ヒーロー直下に `.anxiety-banner`（「怖い」を、我慢しなくていい。）+ `dental-anxiety.html` ボタン。特徴の後に5診療カード `.service-cards`（services.html#各アンカーへ）。1280px=5列/タブレット3列/767px以下2列を computed style で確認）

### Task 6: トップに予約導線とお知らせ抜粋を追加
- 対象: `index.html`
- やること:
  - Task 2 の予約導線ブロックをお問い合わせセクション付近に挿入。
  - 「お知らせ」最新3件の抜粋ブロック（仮3件・`news.html`へリンク）。
- 完了条件: 予約導線4種＋お知らせ抜粋が表示。仮リンク・仮データは `（仮）`/`data-todo` で識別可能。
- 依存: Task 2
- [x] 完了（2026-06-10: アクセスの後に `.news-list` 抜粋3件（仮・news.htmlへ）、お問い合わせの後に予約導線4種（index はフォーム=#contact）。reserve 4ボタン・news 3件を DOM 確認。EPARK/LINE は data-todo）

---

## フェーズ2: 通院をためらう方へ（最重要・コンセプト体現）

### Task 7: 「通院をためらう方へ」ページ骨組み
- 対象: `dental-anxiety.html`（新規・`PAGE_TEMPLATE.html`複製）
- やること:
  - `<head>`: title `歯医者が怖い・通院をためらう方へ｜辻堂がじゅまる歯科`、description（辻堂×不安キーワード）、canonical、OGP、JSON-LD（`MedicalWebPage` or `Dentist`+`mainEntity`枠）。
  - `<main>` にヒーロー（共感トーン）＋本文セクションの空枠（次Taskで中身）。
- 完了条件: ブラウザ表示でヘッダー/フッター/mobile-bar込み骨組みが出る。コンソールエラーなし。
- 依存: Task 1
- [x] 完了（2026-06-10: `dental-anxiety.html` 作成。head=title/desc/canonical/OGP/JSON-LD(MedicalWebPage)。page-hero(共感トーン)+main空枠。HTTP 200・JSエラーなし確認。Task8 と同時実装）

### Task 8: 「ためらう方へ」本文を文言正本から実装
- 対象: `dental-anxiety.html`, `css/style.css`（`/* ためらう方へ */`）
- やること: `codex-dental-copy-audit.md` の「3. 歯医者が怖い方へ」を正本に、共感リード→お気持ち4項目→4つの約束（痛み/カウンセリング/説明/中断OK）→来院ステップ→CTA を実装。断定トーン（「します」「しません」「約束します」）。
  - 不安解消Q&A（FAQ）を本ページにも数件配置（後述Task 19のFAQと内容整合）。
  - CTA文言: 「治療するかどうかは、来てから一緒に決めましょう。」＋予約導線。
- 完了条件: 文言正本の要素が反映され、トーンが「責めない・寄り添う」。`.fade-in` が機能。レスポンシブ崩れなし。
- 依存: Task 7
- [x] 完了（2026-06-10: 文言正本「3.歯医者が怖い方へ」を辻堂ローカライズで実装。共感リード→お気持ち4項目→4つの約束→来院4ステップ→不安解消Q&A4件→CTA「治療するかどうかは、来てから一緒に決めましょう。」。断定トーン。767px以下1列確認。`.fade-in` は本番ブラウザで機能（preview sandbox は IO/screenshot 不可＝環境制約）。地名は辻堂に統一）

---

## フェーズ3: 診療案内（SEOの主戦場）

### Task 9: 診療案内ページ骨組み（5診療の親ページ）
- 対象: `services.html`（新規）
- やること:
  - 5診療（一般歯科/小児歯科/予防歯科/歯周病治療/ホワイトニング）を**1ページ内アンカーセクション**で構成（`#general` `#pediatric` `#prevention` `#perio` `#whitening`）。各セクションに `<h2>`。
  - `<head>`: title `診療案内（一般・小児・予防・歯周病・ホワイトニング）｜辻堂がじゅまる歯科`、description に「辻堂 歯医者」想定キーワード。
- 完了条件: 5セクションの目次＋アンカージャンプが機能。骨組み表示。
- 依存: Task 1
- [ ] 完了

### Task 10: 各診療セクションの本文＋「辻堂×診療」SEO組み込み
- 対象: `services.html`, `css/style.css`（`/* 診療案内 */`）
- やること:
  - 5診療それぞれに、文言正本「9-13. 診療案内ページ」のリード（例 虫歯「削るのは、最後の手段です。」/ 予防「痛くなる前に来る。それが一番の治療です。」/ 小児「嫌がったら、やめます。」/ ホワイトニング「あなたに似合う白さを。」/ 歯周病「歯を失う原因、第1位。でも、防げます。」）＋本文（各3〜5文・仮）。
  - 各 `<h2>` / 本文に「辻堂」地名を自然に織り込み（例「辻堂で予防歯科をお探しの方へ」）。患者語への翻訳（歯周ポケット→歯と歯ぐきの間の溝 等）。
  - 各セクション末にページ別CTA（文言正本「気になることがあれば、お気軽にどうぞ。」）＋予約導線。
- 完了条件: 5診療が出揃い、各見出しに辻堂×診療キーワード。仮本文に `（仮）` 明示。レスポンシブ崩れなし。
- 依存: Task 9
- [ ] 完了

---

## フェーズ4: 院長・当院 / 院内 / 料金 / アクセス / FAQ / お知らせ

### Task 11: 院長・当院紹介ページ
- 対象: `about.html`（新規）, `css/style.css`（`/* 院長・当院 */`）
- やること:
  - 院長メッセージ（文言正本「14. 院長紹介」: 「子どもの頃、歯医者が大嫌いでした。…」を辻堂版にローカライズ）＋経歴枠（仮）＋当院の考え（文言正本「4. 私たちの考え」）。
  - 院長写真は既存 `director-profile.webp` 流用。`○○大学` 等は `（仮）` 維持。
  - JSON-LD `Dentist` の founder/employee枠は確定後とし、ページ内は本文のみ。
- 完了条件: 院長個人トーンのメッセージ＋当院理念が表示。写真表示OK。レスポンシブOK。
- 依存: Task 1
- [ ] 完了

### Task 12: 院内紹介ページ（仮画像プレースホルダー運用）
- 対象: `facility.html`（新規）, `css/style.css`（`/* 院内紹介 */`）
- やること:
  - 受付/待合室/診療室/カウンセリング/設備 の各ブロック（文言正本「15. 院内紹介」: 患者メリット表現「被ばく量が少ないレントゲン」等）。
  - 画像は**全て仮**（`clinic-reception.svg`/`clinic-room.svg`流用＋不足分はSVG/CSSプレースホルダー）。各 `alt` に本番想定文言。`IMAGE_TODO.md` に全枠登録。
  - 「院内写真は内覧会後（約2週間後）に差し替え予定（仮）」の注記。
- 完了条件: 院内各エリアの説明＋仮画像枠が並ぶ。差し替え箇所が `IMAGE_TODO.md` と一致。
- 依存: Task 1, Task 3
- [ ] 完了

### Task 13: 料金表ページ
- 対象: `pricing.html`（新規）, `css/style.css`（`/* 料金表 */`）
- やること:
  - 冒頭文（文言正本「16. 料金表」: 「まずは保険診療で対応します。」）。
  - 料金テーブル（保険診療の目安＋自費メニュー＝ホワイトニング/詰め物等を**仮価格＋（仮）**）。既存 `.info__schedule` テーブルのトンマナ踏襲。
  - 「自費は事前説明・納得後」の明記。
- 完了条件: 料金テーブルが表示。全価格 `（仮）`。レスポンシブで横スクロール or 縦積み対応。
- 依存: Task 1
- [ ] 完了

### Task 14: アクセスページ（既存トップのアクセス資産を発展）
- 対象: `access.html`（新規）, `css/style.css`（`/* アクセス */`）
- やること:
  - 既存トップの `access` セクション（住所・道順・Google Maps iframe・仮注記）をベースに専用ページ化。
  - 道順（辻堂駅南口→徒歩3分・仮）、駐車場（近隣コインパーキング・仮）、診療時間表（既存流用）。
  - NAP（医院名/住所/電話）を**フッターと完全一致**で記載（MEOのNAP統一）。地図は仮位置注記維持。
- 完了条件: 地図・住所・道順・診療時間が表示。NAP表記がフッター/JSON-LDと文字列一致。
- 依存: Task 1
- [ ] 完了

### Task 15: よくある質問ページ
- 対象: `faq.html`（新規）, `css/style.css`（`/* FAQ */`）
- やること:
  - 文言正本「18. FAQ」＋「ためらう方へ」の不安解消Q&Aを統合。カテゴリ: 初めての方/痛み・恐怖/お子さま連れ/費用・保険/予約。
  - 各Q&Aを `<dl>` で構造化（後続TaskでFAQPage JSON-LD化）。断定トーン（「大丈夫です。何年ぶりでも歓迎です。」）。
- 完了条件: カテゴリ別Q&Aが表示。`dental-anxiety.html` のFAQと矛盾なし。
- 依存: Task 1
- [ ] 完了

### Task 16: お知らせ（一覧＋記事雛形）
- 対象: `news.html`（新規・一覧）, `news/PAGE_TEMPLATE_NEWS.html`（新規・記事雛形）
- やること:
  - `news.html`: 日付＋タイトルのリスト（仮3件: 開院準備/内覧会/求人 等）。
  - 記事雛形: 日付・本文の最小構成（マルチページ運用で記事追加しやすく）。
  - トップのお知らせ抜粋（Task 6）とリンク整合。
- 完了条件: 一覧表示＋記事雛形が単体表示可能。仮3件 `（仮）`。
- 依存: Task 1, Task 6
- [ ] 完了

---

## フェーズ5: SEO/MEO 仕上げ・横断整合

### Task 17: 全ページ共通headのSEO要素を統一
- 対象: 全 `*.html`
- やること:
  - 各ページ固有の `<title>` / `<meta description>`（辻堂＋ページ主題キーワード）/ `<link canonical>`（絶対URL）/ OGP（title/description/url、imageは当面共通hero）を設定。
  - `lang="ja"` / viewport / charset の一致確認。
- 完了条件: 全ページで title/description/canonical がユニーク。canonical絶対URLが `https://gajumaru-dental.com/<file>` 形式。
- 依存: Task 4〜16（各ページ存在）
- [ ] 完了

### Task 18: 構造化データ（Dentist）拡張＋FAQPage追加
- 対象: `index.html`, `faq.html`, `dental-anxiety.html`
- やること:
  - トップの `Dentist` JSON-LD に `openingHoursSpecification`（診療時間・仮）/ `geo`枠 / `sameAs`（EPARK・LINE・GBP・確定後）を追加。`telephone`/`streetAddress` は**実NAP確定後に追記**（誤情報を出さない・コメントで明示）。
  - `faq.html` に `FAQPage` JSON-LD（不安解消Q&A）。`dental-anxiety.html` にも主要Q&A分の `FAQPage`。
  - JSON-LD構文を Rich Results 観点でセルフ確認（必須プロパティ充足）。
- 完了条件: 各JSON-LDが妥当なJSON（パースエラーなし）。仮値は未記載、確定待ち箇所をコメント明記。
- 依存: Task 14（NAP表記確定方針）, Task 15
- [ ] 完了

### Task 19: NAP統一・内部リンク・サイトマップ整備
- 対象: 全 `*.html`, `sitemap.xml`（新規）, `robots.txt`（新規）
- やること:
  - 全ページのフッターNAP・電話番号・住所表記を**文字列完全一致**に統一（仮値でも統一）。`_partials.html` を正本に全ページ反映。
  - パンくず or フッターに全ページへの内部リンク（クロール性）。
  - `sitemap.xml`（全URL列挙）＋`robots.txt`（`Sitemap:` 指定）。
- 完了条件: 全ページのNAP一致。sitemap.xmlに全ページ記載。リンク切れなし。
- 依存: Task 4〜16
- [ ] 完了

---

## フェーズ6: 検証・記録

### Task 20: 全ページ横断QA（表示・リンク・レスポンシブ）
- 対象: 全 `*.html`
- やること:
  - 各ページをブラウザで開き、ヘッダー/フッター/mobile-bar一致・ナビ全リンク疎通・`.fade-in`動作・ハンバーガー・スムーススクロール・フォーム（トップ）を確認。
  - 320/375/768/1024/1280px でレスポンシブ崩れ確認。コンソールエラーゼロ。
  - 仮データ（`（仮）`）・仮リンク（`data-todo`）・仮画像の残存箇所を `IMAGE_TODO.md` / `TODO.md` に集約。
- 完了条件: 全ページでJSエラーなし・リンク切れなし・レイアウト崩れなし。仮箇所が台帳に網羅。
- 依存: Task 4〜19
- [ ] 完了

### Task 21: ドキュメント更新・記録
- 対象: `HANDOFF.md`, `TODO.md`, `DECISIONS.md`, `README.md`
- やること:
  - `DECISIONS.md`: 本番マルチページ化の構成（フラットURL構成・共通部品コピー方式）を `[DEC-004]` 記録。
  - `HANDOFF.md`: 現在地（マルチページ化完了・実データ/写真待ち）を更新。
  - `TODO.md`: 残（実NAP差し替え・院内写真差し替え・EPARK/LINE実URL・JSON-LD telephone追記）をバックログ化。
  - `README.md`: ページ一覧・共通部品の同期ルール・仮データ差し替え手順を追記。
- 完了条件: 4ファイル更新済み。次セッションがHANDOFFだけで再開可能。
- 依存: Task 20
- [ ] 完了

---

## 残課題（本計画スコープ外・実データ待ち）

- 実NAP（電話/正式住所/正式診療時間）確定 → 全ページ＋JSON-LD一括差し替え
- 院内写真（約2週間後・内覧会後）→ `IMAGE_TODO.md` 参照で差し替え
- EPARK / LINE の実URL → `data-todo` 箇所を実リンク化
- 予約SaaS（後回し）導入是非
- GBP（Googleビジネスプロフィール）開設 → `sameAs` / MEO本番運用
- リポ名 `gajumaru-dental` rename 検討（任意・URL自動リダイレクト）
